<Controls:MetroWindow x:Class="Currency.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Currency" Height="350" Width="525"
                      
        BorderThickness="0" 
        GlowBrush="Black"
        ResizeMode="CanResizeWithGrip"

        WindowTransitionsEnabled="False"
        WindowStartupLocation="CenterScreen">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Margin="30 15 30 5" Text="Банк" FontSize="26"/>
        <ComboBox Grid.Row="1" Grid.ColumnSpan="3" 
                  Margin="30 5" ItemsSource="{Binding og}"
                  SelectedItem="{Binding Selected}"/>

        <Controls:FlipView x:Name="FlipViewTest"
                           Grid.Row="2"
                           Grid.ColumnSpan="3"
                           Foreground="{DynamicResource WhiteBrush}"
                           Height="200"
                           ItemsSource="{Binding Selected.curr}"
                           SelectionChanged="FlipView_SelectionChanged">
            <Controls:FlipView.ItemTemplate>
                <DataTemplate>
                    
                        <Label Content="{Binding Path=Name}" FontSize="32"/>
                    
                </DataTemplate>
            </Controls:FlipView.ItemTemplate>
            </Controls:FlipView>


    </Grid>
</Controls:MetroWindow>
